name: WorldGuard
main: com.rustret.worldguard.WorldGuard
version: "1.0.0"
api: ["1.0.14"]
description: Plugin for protecting players area

permissions:
  worldguard.god:
    description: "Allows to delete others regions, removes region size limit"
    default: false
  worldguard.flag:
    description: "Gives access to /flag"
    default: false